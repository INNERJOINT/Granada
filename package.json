{
  "name": "@zeonic/zaku",
  "version": "4.0.0",
  "description": "Mass-production dev weapon — AOSP recon, JIRA elimination, Git ammo supply",
  "main": "bridge/mcp-server.cjs",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/INNERJOINT/Granada.git"
  },
  "scripts": {
    "build": "npm run build:hooks",
    "typecheck:hooks": "tsc -p tsconfig.json --noEmit",
    "build:hooks": "tsc -p tsconfig.json",
    "verify:hooks-package": "node scripts/verify-hooks-package.js",
    "test": "npm run build && vitest run",
    "test:watch": "vitest",
    "prepack": "npm run build"
  },
  "files": [
    ".claude-plugin/",
    ".mcp.json",
    "AGENTS.md",
    "CHANGELOG.md",
    "CLAUDE.md",
    "LICENSE",
    "README.md",
    "agents/",
    "assets/",
    "bridge/",
    "dist/",
    "hooks/",
    "output-styles/",
    "scripts/hooks/",
    "skills/",
    "!skills/**/.omc/**"
  ],
  "devDependencies": {
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/exec": "^7.0.3",
    "@semantic-release/git": "^10.0.1",
    "@types/node": "^25.9.1",
    "conventional-changelog-conventionalcommits": "^8.0.0",
    "semantic-release": "^24.2.3",
    "typescript": "^6.0.3",
    "vitest": "^4.1.6"
  }
}
